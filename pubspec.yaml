name: ouiborne
description: application de caisse.
publish_to: 'none'
version: 1.0.12+12
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  pdf: ^3.10.0
  printing: ^5.11.0
  csv: ^6.0.0
  blue_thermal_printer: ^1.2.3  # Vérifiez la dernière version sur pub.dev
  permission_handler: ^11.0.0   # Pour gérer les permissions Bluetooth au runtime


  universal_html: ^2.2.4
  # --- Firebase ---
  flutter_image_compress: ^2.3.0
  firebase_core: ^3.15.2
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.0
  cloud_functions: ^5.1.0


  fl_chart: ^0.69.0


  # --- UI & UX ---
  flutter_colorpicker: ^1.0.0
  auto_size_text: ^3.0.0
  scrollable_positioned_list: ^0.3.8
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.3.2
  scroll_to_index: ^3.0.1
  shimmer: ^3.0.0

  # --- Impression ---

  flutter_staggered_animations: ^1.1.1

  # --- État & Navigation ---
  provider: ^6.1.2
  intl: ^0.20.2
  go_router: ^16.3.0

  # --- Utilitaires ---
  image_picker: ^1.1.2
  uuid: ^4.4.0
  collection: ^1.18.0
  shared_preferences: ^2.2.3
  package_info_plus: ^4.2.0
  http: ^1.1.2
  path_provider: ^2.1.2
  open_filex: ^4.3.4
  esc_pos_printer_plus: ^0.1.1
  esc_pos_utils_plus: ^2.0.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  msix: 3.16.12

msix_config:
  display_name: "Ouiborne"
  publisher_display_name: "Ouiborne"
  identity_name: "Ouiborne.OUIBORNE"
  publisher: "CN=A25805F6-1384-40F1-B42A-44F83F8024DC"
  msix_version: 1.0.12.0
  architecture: x64
  capabilities: internetClient, privateNetworkClientServer
flutter:
  uses-material-design: true